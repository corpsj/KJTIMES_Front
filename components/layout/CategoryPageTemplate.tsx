import { Container, Title, Text } from "@mantine/core";

export default function CategoryPage() {
    return (
        <Container size="xl" py="xl">
            <Title order={1}>섹션 페이지</Title>
            <Text c="dimmed">이 페이지는 준비 중입니다.</Text>
        </Container>
    );
}
